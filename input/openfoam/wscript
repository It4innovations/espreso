
source_files = (
    "openfoam.cpp",
)

def configure(ctx):
    pass

def build(ctx):
    ctx.lib(
        source          = source_files,
        target          = "esinput_openfoam",
        use             = "espreso_includes",
        install_path    = ctx.LIBRARIES
    )
