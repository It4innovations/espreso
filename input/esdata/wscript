
source_files = (
    "esdata.cpp",
)

def configure(ctx):
    pass

def build(ctx):
    ctx.lib(
        source          = source_files,
        target          = "esinput_esdata",
        use             = "espreso_includes",
        install_path    = ctx.LIBRARIES
    )
