
ASSEMBLER = "assembler"

source_files = (
    #"linear/linear.cpp",
    #"linear/elasticity/linearelasticity.cpp"
)

def configure(ctx):
    ctx.setenv(ASSEMBLER, ctx.all_envs[""].derive());

def build(ctx):
    ctx.env = ctx.all_envs[ASSEMBLER]
    return

    ctx.lib(
        source          = source_files,
        target          = "esassembler",
        use             = "espreso_includes",
        install_path    = ctx.LIBRARIES
    )
