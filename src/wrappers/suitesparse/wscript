
def options(opt):
    pass

def configure(ctx):
    headers = "cholmod.h"
    libs = ["cholmod"]
    ctx.check_cxx(mandatory=False, msg="Checking for 'suitesparse'", okmsg=" ".join(libs), define_name="HAVE_SUITESPARSE", header_name=headers, lib=libs, uselib_store="SUITESPARSE")
