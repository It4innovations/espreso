#!/bin/bash

ROOT=`pwd`

if [ "$#" -eq 1 ]; then
  ROOT=$1
fi


export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$ROOT/libs
echo "LD_LIBRARY_PATH +=" $ROOT/libs

export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$ROOT/libespreso
echo "LD_LIBRARY_PATH +=" $ROOT/libespreso

export PATH=$PATH:$ROOT
echo "PATH +=" $ROOT